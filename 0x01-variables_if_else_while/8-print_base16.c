#include <stdio.h>

/**
 * main - prints all the numbers of base 16 in lowercase,
 * followed by a new line
 * Return: 0 always
 */
int main(void)
{
	int z;
	char ch;
	for (z = 48; z < 58; z++)
		putchar(n);
	for (ch = 'a'; ch = 'f'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
