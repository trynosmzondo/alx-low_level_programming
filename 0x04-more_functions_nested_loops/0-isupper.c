#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: 1 .
 */
int _isupper(int c)
{
	if (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	else if (c = 'a', c <= 'z', c++) 
	{
		return (0);
	}
}
